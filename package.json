{
  "name": "presentaunamico",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run schema && astro build",
    "preview": "astro preview",
    "check": "tsc --noEmit",
    "schema": "tsx scripts/gen-json-schema.ts",
    "validate": "tsx scripts/validate.ts",
    "data": "tsx scripts/build-dataset.ts"
  },
  "//": "Tutto cio' che serve a `npm run build` sta in dependencies e non in devDependencies: alcune piattaforme di build installano con NODE_ENV=production, e in quel caso le devDependencies vengono saltate e la build fallisce. In devDependencies restano solo i pacchetti usati da `npm run check`.",
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@resvg/resvg-js": "^2.6.2",
    "@tailwindcss/vite": "^4.1.3",
    "astro": "^5.6.1",
    "satori": "^0.29.0",
    "tailwindcss": "^4.1.3",
    "tsx": "^4.19.3",
    "zod": "^3.24.2",
    "zod-to-json-schema": "^3.24.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "@types/node": "^26.2.0",
    "typescript": "^5.8.2"
  }
}
